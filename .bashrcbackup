#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

neofetch

export PATH=$PATH:/usr/local/go/bin
export GOPATH=/home/payload/go
export GO111MODULE=auto
